{
    "title": "Tomba Clearbit-Company Actor Input",
    "description": "Input schema for the Tomba Clearbit-Company Actor that enriches company information using the Tomba API based on domain names.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "tombaApiKey": {
            "title": "Tomba API Key",
            "type": "string",
            "description": "Your Tomba API key (ta_xxxx). Get it from https://app.tomba.io/api",
            "editor": "textfield",
            "isSecret": true,
            "example": "ta_xxxxxxxxxxxxxxxxxxxx",
            "prefill": "ta_xxxxxxxxxxxxxxxxxxxx"
        },
        "tombaApiSecret": {
            "title": "Tomba Secret Key",
            "type": "string",
            "description": "Your Tomba secret key (ts_xxxx). Get it from https://app.tomba.io/api",
            "editor": "textfield",
            "isSecret": true,
            "example": "ts_xxxxxxxxxxxxxxxxxxxx",
            "prefill": "ts_xxxxxxxxxxxxxxxxxxxx"
        },
        "domains": {
            "title": "Domains to Enrich",
            "type": "array",
            "description": "Array of domain names to enrich with company information.",
            "editor": "stringList",
            "items": {
                "type": "string"
            },
            "example": ["tomba.io", "example.com", "google.com"],
            "prefill": ["tomba.io", "stripe.com"]
        },
        "maxResults": {
            "title": "Maximum Results",
            "type": "integer",
            "description": "Maximum number of companies to enrich. Default is 50.",
            "editor": "number",
            "minimum": 1,
            "maximum": 1000,
            "default": 50,
            "example": 50
        }
    },
    "required": ["tombaApiKey", "tombaApiSecret", "domains"]
}
